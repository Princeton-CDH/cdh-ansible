---
- name: Converge default scenario (cleanup_enabled)
  hosts: all
  tasks:
    - name: "Include finalize_deploy role"
      ansible.builtin.include_role:
        name: finalize_deploy
      vars:
        deploy: "{{ install_root }}/4.1.0-current"
        symlink: "test_app"
