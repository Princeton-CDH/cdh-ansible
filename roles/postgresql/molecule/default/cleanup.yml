---
- name: Cleanup
  hosts: localhost
  gather_facts: false
  tasks:
    - name: remove testing postgres container
      community.docker.docker_container:
        name: test_postgres
        state: absent
        container_default_behavior: no_defaults
