---
- name: Verify
  hosts: instance
  gather_facts: false
  tasks:
    - name: Get a list of current Solr collections
      uri:
        url: "{{ solr_url }}admin/collections?action=LIST"
      register: solr_collections_list

    - name: output solr collections
      debug:
        msg: solr_collections_list
