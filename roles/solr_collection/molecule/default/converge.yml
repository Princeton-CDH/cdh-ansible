---
- name: Converge
  hosts: solr
  vars_files:
    - ../../vars/test.yml
  become: true
  pre_tasks:
    - name: update cache
      apt:
        update_cache: true
        cache_valid_time: 600
  tasks:
    - name: include solr collection role
      include_role:
        name: solr_collection
