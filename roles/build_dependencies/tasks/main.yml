---
- name: install configured dependencies
  apt:
    name: "{{ configured_dependencies }}"
    state: present
    update_cache: true
  changed_when: false

- name: configure tmux
  copy:
    src: "tmux.conf"
    dest: "/etc/tmux.conf"
