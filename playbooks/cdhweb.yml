---
# by default this playbook runs in the qa environment
# to run in production, pass '-e runtime_env=prod'
- name: build the cdh website
  hosts: cdhweb_{{ runtime_env | default('qa') }}
  connection: ssh
  remote_user: pulsys
  roles:
    - create_deployment
    - deploy_user
    - build_dependencies
    - build_project_repo
    - postgresql
    - passenger
    - build_npm
    - configure_logging
    - django
    - django_compressor
    - finalize_deploy
    - close_deployment
