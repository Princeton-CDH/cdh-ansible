- hosts: simulatingrisk
  connection: ssh
  remote_user: pulsys
  become: true
  roles:
    - create_deployment
    - build_dependencies
    - install_app_config
    - build_project_repo
    - python
    - configure_logging
    - passenger
    - finalize_deploy
    - close_deployment
