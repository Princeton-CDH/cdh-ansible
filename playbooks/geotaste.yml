- hosts: geotaste
  connection: ssh
  remote_user: pulsys
  become: true
  roles:
    - create_deployment
    - build_dependencies
    - install_app_config
    - build_project_repo
    - python
    - configure_logging
    - passenger
    - geotaste_setup
    - finalize_deploy
    - close_deployment
