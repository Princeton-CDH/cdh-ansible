- hosts: pemm_qa
  connection: ssh
  remote_user: pulsys
  become: true
  roles:
    - create_deployment
    - build_dependencies
    - install_app_config
    - build_project_repo
    - build_virtualenv
    - configure_apache
    - finalize_deploy
    - close_deployment

# cron tab management still to do (and solr?)


